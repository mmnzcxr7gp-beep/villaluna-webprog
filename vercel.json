{
  "buildCommand": "cd villaluna-client && npm install && npm run build",
  "outputDirectory": "villaluna-client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
